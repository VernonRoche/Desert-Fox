{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf js",
    "compile": "yarn clean && tsc",
    "start": "yarn compile && node js/main",
    "test": "yarn compile && mocha js/test --recursive --exit"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/mocha": "^9.1.0",
    "@types/yargs": "^17.0.9",
    "mocha": "^9.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "express": "^4.17.3",
    "yargs": "^17.3.1"
  }
}
